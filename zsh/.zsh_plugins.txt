# Antidote plugin list - https://antidote.sh
# After editing, restart the shell (antidote re-bundles automatically).

# Handles compinit with caching; must load before completion consumers
mattmc3/ez-compinit

# Extra completions, added to fpath before compinit runs
zsh-users/zsh-completions kind:fpath path:src

# Fish-like inline suggestions from history
zsh-users/zsh-autosuggestions

# Syntax highlighting - keep last
zdharma-continuum/fast-syntax-highlighting
